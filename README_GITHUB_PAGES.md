# AgroConecta Sur - GitHub Pages

El logo de INFIHUILA quedó incrustado directamente en los HTML como Data URI para evitar fallas de ruta en GitHub Pages. También se conserva `assets/logo-infihuila.png`.

Sube todos los archivos y la carpeta `assets` a la raíz del repositorio.
